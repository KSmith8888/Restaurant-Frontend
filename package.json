{
    "name": "restaurant-frontend",
    "version": "1.0.0",
    "description": "The public portion of a restaurant website and management app",
    "private": "true",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "npx tsc"
    },
    "author": "",
    "devDependencies": {
        "typescript": "^5.0.3"
    }
}
